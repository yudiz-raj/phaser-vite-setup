const width = 1080;
const height = 1920;
const config = {
    width: width,
    height: height,
    centerX: width / 2,
    centerY: height / 2,
    version: 1.0,
    fontFamily: 'fontFamily_1',
};

export default config;